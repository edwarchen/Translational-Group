# Changelog

## [1.0.0] - 2026-06-04

### Added
- Marked the current TCR/BCR primer evaluation pipeline as the Version 1.0 baseline.
- Recorded the baseline version in package metadata, runtime package version, and a repository-level `VERSION` file.

### Notes
- Use the Git commit and an annotated Git tag such as `v1.0.0` to preserve this baseline before future development.
